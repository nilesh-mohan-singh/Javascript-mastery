// forEach
// map
// filter
// reduce
// custom callbacks

// sort()
// find()

let numbers = [1, 2, 3, 4, 5] 
let number = numbers.forEach((num) => {
    console.log(num)
})

// numbers.forEach(function (num) {
//     console.log(num);
// })

// numbers.forEach(function num (num) {
//     console.log(num);
// })



let allNumbers = [1, 2, 3 , 4, 5]
let doubled = numbers.map ((num) => {
    console.log(doubled);
})